// BILL 1
const bill = 275;
const tip = bill > 50 && bill < 300 ? (15 * bill) / 100 : (20 * bill) / 100;

console.log(`The bill was ${bill}, the tip was ${tip}, and the total value ${bill + tip}`);

// BILL 2
const bill2 = 40;
const tip2 = bill2 > 50 && bill2 < 300 ? (15 * bill2) / 100 : (20 * bill2) / 100;

console.log(`The bill was ${bill2}, the tip was ${tip2}, and the total value ${bill2 + tip2}`);

// BILL 3
const bill3 = 430;
const tip3 = bill3 > 50 && bill3 < 300 ? (15 * bill3) / 100 : (20 * bill3) / 100;

console.log(`The bill was ${bill3}, the tip was ${tip3}, and the total value ${bill3 + tip3}`);